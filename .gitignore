# Secrets & credentials — NEVER commit these
.env
.env.local
*.env
credentials.json
*credentials*.json
secrets.*

# n8n local data (may contain encrypted credentials & keys)
.n8n/
n8n-data/
database.sqlite

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log
node_modules/
